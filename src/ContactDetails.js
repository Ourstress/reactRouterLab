import React from 'react';

const ContactDetails = (props) => {
    return (
        <div>
            This is page
            {props.username}
        </div>
    );
}

export default ContactDetails;
